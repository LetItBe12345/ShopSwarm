# M2.1.4 配置 Jev 中转站与使用文档：已确认选择

日期：2026-09-23。对应任务：[M2.1.4 配置 Jev 中转站与使用文档](../TODO/done/M2.1.4-配置Jev中转站与使用文档.md)。

用户要求把已实测的兔子 API Jev 线路配置进项目，并形成独立使用文档。项目默认使用 Base URL `https://api.tu-zi.com`、原生接口 `POST /v1/systemone`、模型 `jev-1.13`。站点 API Key 只放在本机 `.env` 或进程环境的 `JEV_API_KEY` 中，不提交具体值。之前 M2.1 的 TypeSafe 直连地址和 `jev-latest` 只保留为历史核对结果，不再是当前默认配置。

依据：[中转站 Jev-1.13 文档](https://api.tu-zi.com/docs/models/jev-1-13)；2026-09-23 已用用户提供的站点 Key 通过最小脱敏请求和 ShopSwarm 请求构造/响应解析各验证一次。完整购物任务仍需 M2.2—M2.3 实测。
