# M2.2 实现完成检查和失败返回

状态：done。建立日期：2026-09-23；完成日期：2026-09-23。

- 对应规划：[roadmap](../../roadmap.md) 的 M2.2，保留原子任务编号。
- 前置条件：M2.1。
- 设计依据：[架构与文件规划](../../DOC/项目架构与文件规划.md)、[六阶段设计记录](../../DOC/购物比价项目设计.md)、[Jev 与执行层设计](../../DOC/分支-购物比价项目设计.md)。历史建议和示例接口不代表已确认或已验证结果。
- 已确认口径：[M2.2 决策](../../decision/M2.2-实现完成检查和失败返回.md)。

## 范围

本任务负责“实现完成检查和失败返回”，以以下 roadmap 编号条目为边界。产品规则与接口约定写入 DOC；这里记录执行动作及验证结果。

## 执行清单

- [x] M2.2.1 选择 `DONE` 后执行独立检查。型号、规格或报价证据不足时不能返回成功。
- [x] M2.2.2 卡住、超时、登录失效或出现验证码时，返回结构化原因和当前进度，由 DSH 或用户继续处理。

执行说明：候选报价必须核对字段证据，页面或规格变化后重新检查进度。

## 验证方式

覆盖伪 DONE、错误规格、缺价、明确阻塞、单条命令超时、登录失效及验证码，验证未误报成功。按已确认决策，暂不加入任务级预算耗尽状态。

执行时使用本项目建立的命令与实际依赖版本，不套用参考仓库测试命令。记录命令、环境、结果及证据；失败检查修复后重跑。

## 完成条件

- [x] 本文件编号条目全部完成，验证方式中的检查均有结果和证据。
- [x] 影响本任务完成的待确认项已解决，失败项已修复并复验，未验证内容未写成支持承诺。
- [x] 同步 roadmap 对应条目与证据链接；整个 M2 阶段仍需 M2.3、M2.4。
- [x] 满足以上条件后移入 `TODO/done/`，同步 roadmap 与 TODO 索引的文件路径。

## 执行与验证记录

2026-09-23：实现见 [`src/shopping/run.ts`](../../src/shopping/run.ts)、[`verify.ts`](../../src/shopping/verify.ts)、[`extract.ts`](../../src/shopping/extract.ts) 和 DSH 工具入口 [`src/index.ts`](../../src/index.ts)。接口与状态见 [接口与数据结构](../../DOC/接口与数据结构.md#m22-单站研究与完成检查)，已确认口径见 [决策记录](../../decision/M2.2-实现完成检查和失败返回.md)。

- [`tests/shopping-verification.test.ts`](../../tests/shopping-verification.test.ts) 覆盖伪规格选择、错误型号/卖家、无价、字段证据及价格变化；[`tests/shopping-run.test.ts`](../../tests/shopping-run.test.ts) 覆盖重复伪 `DONE`、重开后错误规格、`BLOCKED`、登录、验证码、浏览器超时；[`tests/shopping-replay-browser.test.ts`](../../tests/shopping-replay-browser.test.ts) 用实际 agent-browser 在本地商品页打开两次，验证独立会话重开与关闭；[`tests/shopping-extract.test.ts`](../../tests/shopping-extract.test.ts) 检查模型响应解析与用量记录。DSH 工具注册与缺少 Agent 身份拒绝见 [`tests/plugin.test.ts`](../../tests/plugin.test.ts)。
- 环境：Ubuntu 24.04 x86_64、Node.js 24.21.0、agent-browser 0.38.1、Vitest 4.0.18。分别运行 `node_modules/.bin/tsc -p tsconfig.json --noEmit`、`node_modules/.bin/tsc -p tsconfig.scripts.json --noEmit`、`node_modules/.bin/vitest run`、`node_modules/.bin/tsc -p tsconfig.json`，均通过；全套 46 项测试通过。`PATH` 指向本机 Node.js 24.21.0。
- 执行 `node --env-file-if-exists=.env --import tsx scripts/smoke-shopping.ts`，本地商品页使用真实 Jev 中转站与 DeepSeek，最终返回 `success/completed`：1 次 Jev 调用、2 次字段提取、Jev 耗时 976 ms、字段提取耗时合计 1811 ms，字段提取输入 590、输出 156 Token；费用响应未知。前几次尝试因 DeepSeek 默认思考模式达到输出上限及规格证据提取不稳定失败；禁用思考模式、接受当前下拉值的可核实证据后复测成功。不能从一次通过推断成功率。
- 执行 `node --import tsx scripts/smoke-shopping-tool.ts`，通过 DSH ToolRuntime 调用本地登录页，得到 `blocked/login_required` 与非空 `userMessage`；`agent-browser session list --json` 返回空会话。未验证真实站点登录、真实站点字段、DSH 自然语言回复或多站并发，这些留在 M2.3/M3。

真实凭据、Cookie、Profile 与敏感日志未放入仓库。
