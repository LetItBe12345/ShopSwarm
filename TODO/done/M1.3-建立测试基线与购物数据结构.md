# M1.3 建立测试基线与购物数据结构

状态：done。建立及完成日期：2026-09-23。

- 对应规划：[roadmap](../../roadmap.md) 的 M1.3，保留原子任务编号。
- 前置条件：M1.1；会话样例依赖 M1.2。
- 设计依据：[架构与文件规划](../../DOC/项目架构与文件规划.md)、[六阶段设计记录](../../DOC/购物比价项目设计.md)、[Jev 与执行层设计](../../DOC/分支-购物比价项目设计.md)。历史建议和示例接口不代表已确认或已验证结果。
- 已确认选择：[M1.3 决策记录](../../decision/M1.3-建立测试基线与购物数据结构.md)。

## 范围

本任务负责“建立测试基线与购物数据结构”，以以下 roadmap 编号条目为边界。产品规则与接口约定写入 DOC；这里记录执行动作及验证结果。

## 执行清单

- [x] M1.3.1 用本地测试页面覆盖搜索、规格选择、延迟加载、遮挡和失效元素，同时记录启动、快照和动作耗时。
- [x] M1.3.2 定义最小 `ProductIdentity`、`Offer`、`Evidence` 字段，为后续验证提供稳定输入输出。

执行说明：按 [M1.3 决策](../../decision/M1.3-建立测试基线与购物数据结构.md) 实现。五类场景各自使用 `tests/fixtures/pages/baseline-*.html`，报价类型和 `validateOffer` 放在 `src/types.ts`。至少 10 个任务、金额样例和真实站点仍归 Q.1。

## 已确认实现约束

- 五个场景分五页，测试在 `127.0.0.1` 临时端口驱动；只检查快照和失效引用，不从页面抽取报价。
- 报价只含载体字段。标价和费用金额用 `known` 或 `unknown`，金额用整数最小单位。看不到卖家时不写未知卖家报价。
- `open`、`snapshot`、`action` 三段耗时在测试内记录。只断言三段存在且毫秒数为非负整数，不设时延阈值，也不改适配层返回值。

## 验证方式

重复执行搜索、选规格、延迟、遮挡和失效引用任务；用代码构造的报价检查已知金额和未知运费都能通过 `validateOffer`。

执行时使用本项目建立的命令与实际依赖版本，不套用参考仓库测试命令。记录命令、环境、结果及证据；失败检查修复后重跑。

## 完成条件

- [x] 本文件编号条目全部完成，验证方式中的检查均有结果和证据。
- [x] 影响本任务完成的待确认项已解决，失败项已修复并复验，未验证内容未写成支持承诺。
- [x] 同步 roadmap 对应条目与证据链接；M1 阶段仍等待 M1.4 验收。
- [x] 满足以上条件后移入 `TODO/done/`，同步 roadmap 与 TODO 索引的文件路径。

## 执行与验证记录

### 2026-09-23 执行与验证

- 实现：[购物数据类型与运行时校验](../../src/types.ts)、[M1.3 浏览器及数据测试](../../tests/shopping-baseline.test.ts)、[五个本地测试页面](../../tests/fixtures/pages/)；数据契约见[接口与数据结构](../../DOC/接口与数据结构.md)。
- 环境：Ubuntu 24.04.4 LTS x86_64、Node.js 24.21.0、pnpm 11.7.0、agent-browser 0.38.1。
- 命令：`npm exec --yes --package=node@24.21.0 --package=pnpm@11.7.0 -- pnpm check`。
- 结果：类型检查通过；3 个测试文件共 13 项测试通过；TypeScript 构建通过。五个页面均通过本地 `127.0.0.1` 临时端口驱动。测试日志记录 `open`、`snapshot` 和每次动作的阶段名、耗时、命令、Node.js/平台、agent-browser 版本与页面标识；记录值不作为性能阈值。
- 报价校验覆盖已知金额、未知运费、字段路径错误、金额币种不一致、非安全整数及重复费用种类。未实现页面报价抽取、商品匹配、优惠规则、最终价格计算或证据完整性判断；这些仍按 Q.1/M4 范围处理。
- 遮挡场景用 `inert` 控制可访问树；失效引用场景在元素移除后刷新快照，再验证旧引用返回 `stale_element_reference`。没有访问真实购物站点或使用用户 Profile。
