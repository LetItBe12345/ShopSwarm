# M1.2 管理执行预算与后台会话

状态：done。建立日期：2026-09-23；完成日期：2026-09-23。

- 对应规划：[roadmap](../../roadmap.md) 的 M1.2，保留原子任务编号。
- 前置条件：M1.1。
- 设计依据：[架构与文件规划](../../DOC/项目架构与文件规划.md)、[六阶段设计记录](../../DOC/购物比价项目设计.md)、[Jev 与执行层设计](../../DOC/分支-购物比价项目设计.md)。历史建议和示例接口不代表已确认或已验证结果。
- 已确认选择：[M1.2 决策记录](../../decision/M1.2-管理执行预算与后台会话.md)。

## 范围

本任务负责“管理执行预算与后台会话”，以以下 roadmap 编号条目为边界。产品规则与接口约定写入 DOC；这里记录执行动作及验证结果。

## 执行清单

- [x] M1.2.1 等待使用可观察条件及超时。单条命令保持 30 秒超时。不设任务级总时长、动作次数和连续失败限制。
- [x] M1.2.2 验证独立后台会话、真实 Profile 的快照复制和资源清理。默认 headless，不连接正在使用的 Chrome。

执行说明：M1.2 会话快照复制本机 Chrome 目录 `Default`（光浩金，`guanghaojin56@gmail.com`）。不复制其他 Profile，不构建空白 Profile。`shopswarm_diagnose` 不加载该 Profile。记录会话归属，不记录预算。

## 已确认实现约束

- 等待只接受 `text`、`url`、`load`，超时不超过 30 秒。
- 一次执行一个会话；结束或取消时只关闭本会话。
- 复制 `Default` 的登录态。成功看临时副本里有 `jd.com` 或 `tmall.com` 记录，并且 ShopSwarm 不写入源 Profile。

## 验证方式

覆盖单次等待超时、单条命令超时、取消、双会话隔离及清理。确认副本含有京东或天猫记录，源 Profile 未被 ShopSwarm 写入，临时副本在关闭后消失。有图形会话时观察前台焦点。

执行时使用本项目建立的命令与实际依赖版本，不套用参考仓库测试命令。记录命令、环境、结果及证据；失败检查修复后重跑。

## 完成条件

- [x] 本文件编号条目全部完成，验证方式中的检查均有结果和证据。
- [x] 影响本任务完成的待确认项已解决，失败项已修复并复验，未验证内容未写成支持承诺。
- [x] 同步 roadmap 对应条目与证据链接；整个阶段须等待该阶段实现和验收全部完成。
- [x] 满足以上条件后移入 `TODO/done/`，同步 roadmap 与 TODO 索引的文件路径。

## 执行与验证记录

### 2026-09-23 执行与验证

- 实现：[agent-browser 会话适配器](../../src/browser/agent-browser-session.ts)、[浏览器类型](../../src/browser/types.ts)；等待必须使用 `text`、`url` 或 `load` 条件并提供正整数超时，超时不得超过会话命令超时。`networkidle` 由调用方显式指定时才使用。单条命令上限为 30 秒；不设任务总时长、动作次数或连续失败限制。
- Profile 只能按 Chrome Profile 目录名指定，拒绝路径；每条浏览器命令显式传 `--headed false`，不受 `AGENT_BROWSER_HEADED` 环境变量影响。`shopswarm_diagnose` 不加载用户 Profile。
- Node.js 24.21.0、pnpm 11.7.0：`npx --yes --package=node@24.21.0 --package=pnpm@11.7.0 --call 'pnpm check'` 通过，类型检查、10 项测试和构建均通过。
- agent-browser 0.38.1 在 Chrome 运行时的并行实测见下条补充记录。之前 Chrome 关闭状态下的快照检查也通过。
- 源目录写入复核：Chrome 关闭后重跑 Profile 快照，用 `strace` 跟踪验证进程文件操作。`Default` 文件清单/大小/修改时间摘要前后相同；源目录写入型系统调用为 0。
- 两个不同会话使用不同 session 标识；关闭一个会话只发出该 session 的 `close`，另一个会话保持可操作。自动化断言见 [浏览器适配器测试](../../tests/browser-adapter.test.ts)。取消传播和命令超时分别由 CLI `AbortSignal` 与每次 `execFile` timeout 实现；本次自动检查未额外等待 30 秒触发超时。
- 取消实测：等待过程中触发 `AbortSignal` 后返回 `uncertain/cancelled`；随后的 `close` 命令报告传输失败，因为 daemon 已退出。复查 `agent-browser session list --json` 返回空，临时 Profile 已删除，没有遗留会话或浏览器进程。清理失败按约定单独返回，没有改写动作结果。
- 焦点检查通过：从 GNOME Shell 进程环境确认桌面为 `DISPLAY=:0`，使用 `/run/user/1000/gdm/Xauthority` 读取活动窗口。Profile 快照打开和关闭前后 `_NET_ACTIVE_WINDOW` 都是 `0x3400007`。
- Chrome 保持前台运行时的并行复核：设置 `AGENT_BROWSER_HEADED=1` 后仍通过 `--headed false` 打开独立 headless 会话，固定页快照成功，Profile 临时副本 Cookies 数据库含有 5 个京东/天猫域名记录。前台 Chrome 进程仍存活；最终一次复测的活动窗口在打开前、会话中、关闭后都为同一用户 Chrome 窗口 `0x3800017`。会话关闭成功，临时副本消失，`agent-browser session list --json` 返回空。
- 用 `strace` 跟踪上述并行复核：ShopSwarm 对 `~/.config/google-chrome/Default` 的源文件只读，没有写入型系统调用。源目录摘要出现变化，来自保持运行的 Chrome 自身更新；这不作为 ShopSwarm 写入证据。
- 真实 Profile 检查没有输出 Cookie 值、账号信息或页面内容。没有遗留未验证项。
