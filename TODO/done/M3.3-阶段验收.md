# M3.3 阶段验收

状态：done。建立日期：2026-09-23。完成日期：2026-09-23。

- 前置条件：M3.1、M3.2。

## 范围

验收 DSH 对同一 Coding Plan 任务的多来源并行执行、资源限制、取消和局部失败。

## 执行清单

- [x] M3.3.1 至少两个 Coding Plan / 中转套餐来源的并行任务与逐个任务使用相同输入，结果可分别追溯。
- [x] M3.3.2 超出资源上限的任务等待，不突破限制。
- [x] M3.3.3 取消整体任务后子任务和所属资源停止；单来源失败时其他来源仍可结束。
- [x] M3.3.4 并行和顺序版本使用同一 Verifier，不因并行切换判断标准。
- [x] M3.3.5 记录总耗时、成功/失败/阻塞数和峰值内存，判断并发带来的实际收益。

## 验证方式

同一任务交替运行顺序与并行版本，检查结果归属、资源上限、取消释放和指标。

## 完成条件

- [x] 所有条目均有可复现记录。
- [x] 同步 roadmap 和 TODO 索引。
- [x] 完成后移入 `TODO/done/`。

## 执行与验证记录

2026-09-23：固定输入沿用 M3.1 的智谱 Coding Plan 与星模 XModel 两个来源；顺序和并行均通过同一个来源任务函数及同一个 Verifier。`ResourceLimit` 单元测试证明 max=1 时第二个调用等待且取消等待不占槽位。`runParallelResearch` 测试证明局部失败不影响另一来源，父取消会停止全部子任务并等待清理。一次本地固定实验（Node.js 24.21.0、pnpm 11.7.0、两来源、maxConcurrent=1）记录顺序 32.71 ms、并行 33.04 ms；顺序成功/失败/阻塞为 1/1/0，并行为 1/1/0；峰值 RSS 分别 91.8 MiB / 92.1 MiB。该实验验证归属、等待和取消路径，不代表真实厂商价格性能收益。M3.1 的真实页面记录仍为阻塞结果，未把未知价格补齐。
