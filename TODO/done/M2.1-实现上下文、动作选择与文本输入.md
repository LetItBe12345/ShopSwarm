# M2.1 实现上下文、动作选择与文本输入

状态：done。建立日期：2026-09-23；完成日期：2026-09-23。

- 对应规划：[roadmap](../../roadmap.md) 的 M2.1，保留原子任务编号。
- 前置条件：M1 阶段验收通过。
- 设计依据：[架构与文件规划](../../DOC/项目架构与文件规划.md)、[六阶段设计记录](../../DOC/购物比价项目设计.md)、[Jev 与执行层设计](../../DOC/分支-购物比价项目设计.md)、[Jev 调用与状态](../../DOC/Jev调用与状态.md)。历史建议和示例接口不代表已确认或已验证结果。

## 范围

本任务负责“实现上下文、动作选择与文本输入”，以以下 roadmap 编号条目为边界。产品规则与接口约定写入 DOC；这里记录执行动作及验证结果。

## 执行清单

- [x] M2.1.1 将目标、约束、完成条件、任务进度、当前页面和近期动作结果组织为有限上下文。
- [x] M2.1.2 根据当前页面生成有效操作和目标集合。Jev 只选择语义动作，执行器负责浏览器命令。
- [x] M2.1.3 `TYPE_TEXT` 优先使用任务中已有的明确内容；需要生成文本时调用文本辅助模型，并单独记录耗时和费用。

执行说明：核对 Jev 请求响应；候选目标来自当前快照，文本辅助调用单独计量。

## 验证方式

用固定快照和可控模型响应验证动态动作集合、上下文更新、非法目标拒绝、明确文本复用及缺失文本处理。

执行时使用本项目建立的命令与实际依赖版本，不套用参考仓库测试命令。记录命令、环境、结果及证据；失败检查修复后重跑。

## 完成条件

- [x] 本文件编号条目全部完成，验证方式中的检查均有结果和证据。
- [x] 影响本任务完成的待确认项已解决，失败项已修复并复验，未验证内容未写成支持承诺。
- [x] 同步 roadmap 对应条目与证据链接；整个 M2 阶段仍需等待 M2.2—M2.4。
- [x] 满足以上条件后移入 `TODO/done/`，同步 roadmap 与 TODO 索引的文件路径。

## 执行与验证记录

2026-09-23：实现见 [`src/jev.ts`](../../src/jev.ts)、[`src/text-input.ts`](../../src/text-input.ts)，接口约定见 [接口与数据结构](../../DOC/接口与数据结构.md#m21-动作选择接口)，已确认选择见 [决策记录](../../decision/M2.1-实现上下文、动作选择与文本输入.md)。

- 固定快照和可控模型响应测试见 [`tests/jev-action.test.ts`](../../tests/jev-action.test.ts)：有限上下文、动态候选、下拉选项、非法目标拒绝、旧页面版本及变动目标拒绝、明确文本复用、缺失文本处理、单独用量记录，8 项通过。实际本地规格页快照包含下拉选项，候选值 `1TB` 和 `2TB` 均生成成功。
- 环境：Ubuntu 24.04 x86_64、Node.js 24.21.0、pnpm 11.7.0、agent-browser 0.38.1。运行 `corepack pnpm typecheck`、`node_modules/.bin/vitest run`、`corepack pnpm build`，分别通过类型检查、30 项测试和构建。执行时使用 Node.js 24.21.0 的 `PATH`；项目内 `pnpm check` 在当前全局 pnpm 入口下不能正确递归调用，所以三段命令分别执行。
- 用脱敏示例任务真实调用 DeepSeek `deepseek-flash`，返回可填写文本，记录输入 165、输出 118 Token；响应没有实际扣费金额，记录为未知。没有配置 `TYPESAFE_API_KEY`，未调用真实 Jev 服务。TypeSafe 的真实响应、计费和网络出口仍需在后续有密钥的验证中核对，不能据此声明真实单站购物已通过。
- 全套测试曾因 M1.4 清理钩子并发关闭浏览器和本地服务器而超时；改为逆序逐项清理后，失败项单独复测及全套测试均通过。当前 agent-browser 活动会话为空。

真实凭据、Cookie、Profile 与敏感日志未放入仓库。
