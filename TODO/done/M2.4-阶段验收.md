# M2.4 阶段验收

状态：done。建立日期：2026-09-23；完成日期：2026-09-23。

- 前置条件：M2.1—M2.3。
- 已确认方向：[M2.4 决策](../../decision/M2.4-阶段验收.md)。
- 产品规则：[Coding Plan 比价规则](../../DOC/Coding%20Plan%20比价规则.md)。

## 范围

M2.4 验收 M2 已实现的浏览器动作循环、Jev 约束和完成检查，并把后续真实领域固定为 Coding Plan / LLM 中转套餐。M2.4 不实现 M4 的 `PlanOffer`、额度换算或最终排序。

## 执行清单

- [x] M2.4.1 固定样例能区分不同模型标识、按量单价、套餐价格和额度；缺价、条件优惠、未知额度和伪完成不会被判为已确认费用。
- [x] M2.4.2 建立可复现的 Coding Plan 用例记录，包含来源、核对日期、成功与失败尝试、未知项和任务要求。
- [x] M2.4.3 模型选择的动作或目标不在当前有效集合时，执行器拒绝操作；Jev 返回后页面或目标变化时也拒绝执行。
- [x] M2.4.4 单条命令超时、取消、明确阻塞、登录、验证码、频控和服务失败均返回结构化原因。
- [x] M2.4.5 明确当前 M2 数据模型的限制；不把计划中的 `PlanOffer` 写成已实现。
- [x] M2.4.6 冻结 required / desired 字段规则并写入领域契约；实际通用字段驱动实现纳入 M4.1。
- [x] M2.4.7 Jev 与 LLM baseline 使用相同任务条件、Action Space 和独立检查；无法取得费用时标为 unknown。
- [x] M2.4.8 真实来源只读；出现风控或页面不可访问时记录失败，不以绕过风控作为阶段通过条件。

## 验证方式

复跑本地正确/错误报价、非法目标、页面变化、明确阻塞、命令超时和取消；再访问公开 Coding Plan 来源做只读记录。真实来源可以失败，但保留失败原因和页面边界。

## 完成条件

- [x] 上述条目全部有测试或脱敏记录。
- [x] 文档明确区分当前 M2 已实现接口和 M4 计划中的 Coding Plan 领域模型。
- [x] 同步 roadmap、TODO 索引和兼容性文档。
- [x] 完成后移入 `TODO/done/`。

## 执行与验证记录

2026-09-23：在独立 worktree、Ubuntu 24.04 x86_64、Node.js 24.21.0、pnpm 11.7.0、agent-browser 0.38.1、Node.js 24.21.0 上执行。`pnpm typecheck`、`pnpm test`（13 个文件、53 项通过）和 `pnpm build` 均通过。根目录 `pnpm check` 在当前 pnpm 配置下报 `packages field missing or empty`，因此按项目已有做法拆开执行三项检查。

- M2.4.1：`tests/provider-benchmark.test.ts` 与 `tests/fixtures/provider-benchmark.json` 覆盖 GLM-5.3 与 GLM-5.3-Flash 区分、按量单价、套餐价格、未知周额度、条件峰谷倍率、缺少缓存价和伪造页面指令；`tests/shopping-verification.test.ts` 覆盖缺价和伪完成。
- M2.4.2 / M2.4.5 / M2.4.8：2026-09-23T21:06:07+08:00 使用独立 headless agent-browser 会话只读访问 [智谱 GLM Coding Plan 套餐概览](https://docs.bigmodel.cn/cn/coding-plan/overview)。页面明确写出 GLM-5.3、GLM-5.3-Flash，Lite/Pro/Max 的 5 小时积分（2,000/12,000/28,000）和每周积分（10,000/60,000/140,000），以及 Input、Cached Input、Output 抵扣系数、工具限制、刷新规则和峰谷条件。页面未出现套餐金额，金额保持 unknown；未登录、未购买、未支付。第二个来源 OpenRouter 页面在加载窗口内超时，记录为失败，未重试或绕过风控。
- M2.4.3 / M2.4.4：`tests/jev-action.test.ts` 覆盖非法目标、页面 revision 变化和目标消失；`tests/shopping-run.test.ts` 覆盖超时、取消、登录、验证码、频控、明确阻塞和服务失败，结果均带结构化 `status` / `reasonCode`。
- M2.4.6：required 字段需要当前页面证据；desired 字段缺失保持 unknown。规则和计划中的 `PlanOffer` 见 [Coding Plan 比价规则](../../DOC/Coding%20Plan%20比价规则.md)，当前代码仍是 M2 的 `ShoppingTask` / `Offer` 历史接口，未宣称已实现 `PlanOffer`。
- M2.4.7：`tests/llm-action.test.ts` 与 M2.3 基线共用 `buildActionRequest`、同一当前页 Action Space、`resolveAction` 和独立完成检查；费用没有供应商实际扣费字段时保持 unknown。

真实凭据、Cookie、Profile 与敏感日志不放入仓库。
