# M2.1.4 配置 Jev 中转站与使用文档

状态：done。建立日期：2026-09-23；完成日期：2026-09-23。对应 [roadmap](../../roadmap.md) 的 M2.1.4；前置条件：M2.1。

## 范围

把已实测的兔子 API Jev 线路配置为 ShopSwarm 默认调用路径，提供不含真实密钥的使用文档和可重复执行的脱敏连通测试。完整购物动作循环仍属于 M2.2—M2.3。

## 执行清单

- [x] 默认 Base URL 为 `https://api.tu-zi.com`，请求路径为 `/v1/systemone`，模型为 `jev-1.13`；密钥从 `JEV_API_KEY` 读取，不写入 Git。
- [x] 兼容中转站的两种 Jev 响应形状，并在超过 32 KiB 请求限制时明确拒绝。
- [x] 在 `DOC/` 写明配置项、调用命令、响应、已验证范围和仍待验证内容；提供脱敏连通命令。

## 验证方式

运行类型检查、定向测试、完整测试和构建；用本机忽略的 `.env` 执行真实 `smoke:jev`，只记录响应模型、操作、耗时和用量，不输出密钥。

## 完成条件

- [x] 上述清单和验证均通过，证据写入本文件。
- [x] 同步 roadmap、TODO 索引和兼容性记录；移入 `TODO/done/`。

## 执行与验证记录

2026-09-23：实现见 [`src/jev.ts`](../../src/jev.ts)、[`scripts/smoke-jev.ts`](../../scripts/smoke-jev.ts)；使用说明见 [Jev 中转站使用](../../DOC/Jev中转站使用.md)，选择依据见 [决策记录](../../decision/M2.1.4-配置Jev中转站与使用文档.md)。

- 在 Node.js 24.21.0、pnpm 11.7.0、Ubuntu 24.04 x86_64 下，`corepack pnpm typecheck`、`node_modules/.bin/vitest run`、`corepack pnpm build` 均通过；共 32 项测试，其中 M2.1 Jev 测试 10 项。固定响应测试覆盖顶层 `answers`、`data.answers` 及 32 KiB 超限拒绝。
- 使用本机 Git 忽略且权限为 600 的 `.env`，运行 `corepack pnpm smoke:jev` 成功：`model=jev-1.13.0`、`operation=DONE`、输入 550 Token、输出 42 Token。多次脱敏请求均成功；最后一次耗时约 2 秒。测试输出未包含 API Key。
- 真实购物站点、完整动作循环和较长页面上下文仍未验证。超过中转站限制的请求会在本地明确拒绝，不宣称已自动压缩到可用大小。
