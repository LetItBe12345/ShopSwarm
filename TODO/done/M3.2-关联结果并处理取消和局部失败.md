# M3.2 关联结果并处理取消和局部失败

状态：done。建立日期：2026-09-23。完成日期：2026-09-23。

- 前置条件：M3.1。

## 范围

保证多个 Coding Plan 来源并行时，Agent、工具调用、浏览器或请求、日志和字段证据不会串任务；一个来源失败不抹掉其他来源的有效结果。

## 执行清单

- [x] M3.2.1 将 Agent、callId、来源、浏览器/HTTP 请求、日志和证据关联到同一子任务。
- [x] M3.2.2 父任务取消时传播取消并释放所属资源。
- [x] M3.2.3 单个来源超时、登录、验证码、频控、字段缺失或服务失败时返回局部结果，其他来源继续。
- [x] M3.2.4 汇总结果保留各来源自己的 unknown、失败原因和采集时间，不用其他来源的数据补齐。

## 验证方式

覆盖整体取消、等待资源时取消、单来源失败、单来源超时和清理失败。检查有效部分结果仍能返回，证据归属不串任务。

## 完成条件

- [x] 所有条目均有测试或记录。
- [x] 同步 roadmap 和 TODO 索引。
- [x] 完成后移入 `TODO/done/`。

## 执行与验证记录

2026-09-23：新增 `src/research/parallel.ts`。每个来源结果保留 `taskId`、`agentId`、`callId`、`source` 和独立取消信号；父信号取消时广播给全部子任务，并等待已启动的 Promise 结束。单来源异常转成该来源的 `failed` 结果，成功来源继续返回；结果值原样保留 unknown 和来源时间戳，不做跨来源补齐。`tests/parallel-research.test.ts` 覆盖来源关联、局部验证码失败、整体取消和两个子任务均收到取消。资源槽位仍由 M3.1 的 `ResourceLimit` 在工具入口持有到清理完成。
