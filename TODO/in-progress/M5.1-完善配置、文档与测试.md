# M5.1 完善配置、文档与测试

状态：in-progress（部分完成）。建立日期：2026-09-23。

- 对应规划：[roadmap](../../roadmap.md)。
- 当前范围：[通用购物任务决策](../../decision/产品范围-通用购物任务.md)。
- 测试策略：[真实购物任务与测试策略](../../DOC/真实购物任务与测试策略.md)。

## 范围

本任务面向通用自然语言购物研究的可安装、可运行和可验证体验。Coding Plan / LLM 套餐继续作为测试域，但不能成为安装文档或发布测试的唯一入口。

## 执行清单

- [ ] M5.1.1 固定依赖版本和配置格式，缺少浏览器、Jev/文本模型凭据或必要来源配置时给出明确诊断。
- [ ] M5.1.2 编写安装、首次自然语言购物任务、取消、清理、真实来源边界和常见故障文档；示例至少包含一个非 Coding Plan 商品。
- [ ] M5.1.3 离线 fixture、模型调用测试和真实来源 E2E 分层；CI 默认不需要真实账户，真实 E2E 按 Q.1 的多领域矩阵显式启用。
- [ ] M5.1.4 文档示例使用脱敏或测试价格，不把静态数字写成当前市场价格，也不把单次站点成功写成平台级支持承诺。

## 验证方式

实际执行文档命令；离线环境验证安装和错误诊断；真实来源测试需显式启用。真实 E2E 优先使用公开、无需登录、风控较低的来源，并保留失败和阻塞记录。

## 完成条件

- [ ] 执行清单全部有结果和证据。
- [ ] 未验证内容不写成支持承诺。
- [ ] 文档入口明确说明 Coding Plan 只是测试域之一。
- [ ] 同步 roadmap 和 TODO 索引。
- [ ] 完成后移入 `TODO/done/`。

## 执行与验证记录

2026-09-23：新增 [GitHub Actions CI](../../.github/workflows/ci.yml)。PR 和 `main` push 运行 `pnpm check` 与 `pnpm smoke:browser`，包含本地固定页面浏览器测试和模拟模型响应测试；Release 发布后才用 Environment Secret 运行真实 Jev、DeepSeek API 的 `pnpm smoke:shopping`。该真实调用仍使用本地页面，尚未覆盖真实多领域购物来源，因此 M5.1.3 保持未完成。

修复根目录 `pnpm-workspace.yaml` 缺少 `packages` 导致 `pnpm check` 无法启动的问题。Node.js 24.21.0、pnpm 11.7.0 下执行 `pnpm install --frozen-lockfile --offline`、`pnpm check`（13 个测试文件、53 项通过）和 `pnpm smoke:browser`（`markerFound: true`）通过。GitHub 托管运行器上的结果与真实 API Release 任务尚待首次运行验证。
